
//Evitar ingresar datos numericos o espacio
var message = parseInt(prompt ("Escriba su mensaje"));

if (message.length === 0 || typeof message ===number  ) {
  console.log (alert ("Datos no validos favor de ingresar otros datos"));}
else if (typeof message === String)
{console.log(message); }
   function cipher (str, number) {
     var alphabetArray = [];
     for (var i=65; i<91; i++) {
       var letter = String
       .fromCharCode(i);
       alphabetArray.push(letter);
       var n = ((alphabetArray.charAt(i) += number)%26)


 //Crea una funcion de descifrar
 function decipher (string, number) {

  //Debe ser capaz de recibir y decifrar  letras mayusculas y minuscula
  else if { for (i=65; i<91; i++) {
     var letter= string.fromCharCode(i);
}
