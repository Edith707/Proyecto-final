
//Crea una funcion de cifrado
function cipher (string, number ) {
  var alphabetArray = [];
  //Si tu programa  no debe recobir  espacios vacios o numeros
  if ( string.length==="0" || typeof string !== "string" ) { return "Favor de ingresar un dato valido"}

  //Debe ser capaz de recibir y decifrar  letras mayusculas y minusculas
  else if { for (i=65; i<91; i++) {
   var letter= string.fromCharCode(i);
   alphabetArray.push(letter);}
    }
  else if {for i=97; i<123; i++} {
    var letter= string.fromCharCode(i);
    alphabetArray.push(letter);}
  }
  
}

 //Crea una funcion de descifrar
 function decipher (string, number) {
  //Si tu programa  no debe recobir  espacios vacios o numeros
  if ( string.length==="0" || typeof string !== "string" ) { return "Undefined";}
  //Debe ser capaz de recibir y decifrar  letras mayusculas y minusculas
  else if { for (i=65; i<91; i++) {
   var letter= string.fromCharCode(i);
   aplhabetArray.push(letter);} }

}
